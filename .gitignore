# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules/
.pnpm-store/
.pnpm/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# lockfile: use npm (package-lock.json) for Netlify compatibility
pnpm-lock.yaml

# testing
/coverage

# next.js
.next/
out/

# embedded Call Ken app (built from real_timecall)
/public/call-ken

# Cambridge listening mp3 (1.3GB — tải bằng script hoặc host riêng)
/public/audios/

# production
/build

# cache / build rác
.turbo/
.cache/
scripts/cache/
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
test.txt
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# env (không đẩy lên GitHub)
.env*
!.env.example

# vercel
.vercel

# Cursor / IDE (tùy chọn — bỏ comment nếu muốn ẩn hẳn)
# .cursor/

# Local Netlify folder
.netlify
.claude/

# Lichess puzzle dataset + local SQLite (regenerable; large)
data/lichess_db_puzzle.csv
data/lichess_db_puzzle.csv.zst
data/puzzles.sqlite
data/puzzles.sqlite-*
data/progress.sqlite
data/progress.sqlite-*
# `puzzles-prod.sqlite` is the deployable subset built by
# scripts/build-puzzles-prod.ts — small enough to commit and bundle into
# Netlify Functions. Only the matching journal/wal sidecars are excluded.
!data/puzzles-prod.sqlite
data/puzzles-prod.sqlite-*
